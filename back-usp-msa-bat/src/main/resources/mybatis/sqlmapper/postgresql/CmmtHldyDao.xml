<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="aicluster.batch.common.dao.CmmtHldyDao">

	<delete id="deleteNotUserDsgn">
		/** CmmtHldyDao.deleteNotUserDsgn */
		delete from cmmt_hldy
		where
			ymd like #{yyyymm} || '%'
	</delete>

	<insert id="insertList">
		/** CmmtHldyDao.insertList */
		insert into cmmt_hldy (ymd, ymd_nm, user_dsgn)
		values
			<foreach item="item" collection="list" separator=",">
			(
				 #{item.ymd}
				,#{item.ymdNm}
				,#{item.userDsgn}
			)
			</foreach>
		on conflict
		do nothing
	</insert>
</mapper>