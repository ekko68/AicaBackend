<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="aicluster.common.common.dao.CmmtBoardArticleUrlDao">

	<sql id="cmmtBoardArticleUrl_cols">
		 url_id
		,article_id
		,sort_order
		,url_nm
		,url
	</sql>

	<insert id="insertList">
		/** CmmtBoardArticleUrlDao.insertList */
		insert into cmmt_board_article_url (
			 url_id
			,article_id
			,sort_order
			,url_nm
			,url
		) values
		<foreach item="item" collection="list" separator=",">
			(
				 #{item.urlId}
				,#{item.articleId}
				,#{item.sortOrder}
				,#{item.urlNm}
				,#{item.url}
			)
		</foreach>
	</insert>

	<select id="selectList" resultType="aicluster.common.common.entity.CmmtBoardArticleUrl">
		/** CmmtBoardArticleUrlDao.selectList */
		select
			<include refid="cmmtBoardArticleUrl_cols"/>
		from cmmt_board_article_url
		where
			article_id = #{articleId}
		order by
			sort_order
	</select>

	<delete id="deleteList">
		/** CmmtBoardArticleUrlDao.deleteList */
		delete from cmmt_board_article_url
		where
			article_id = #{articleId}
	</delete>
</mapper>