<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="aicluster.pms.common.dao.UsptStdrTaxitmSetupDao">

	<insert id="insert">
		/** UsptStdrTaxitmSetupDao.insert */
		insert into uspt_stdr_taxitm_setup
										(
											wct_taxitm_id
											, stdr_bsns_cd
											, wct_ioe_id
											, creator_id
											, created_dt
										)
								values	(
											#{wctTaxitmId}
											, #{stdrBsnsCd}
											, #{wctIoeId}
											, #{creatorId}
											, #{createdDt}
										)
	</insert>

	<delete id="deleteAll">
		/** UsptStdrTaxitmSetupDao.deleteAll */
		delete from uspt_stdr_taxitm_setup
		where
			stdr_bsns_cd = #{stdrBsnsCd}
	</delete>

	<select id="selectList" resultType="aicluster.pms.common.entity.UsptStdrTaxitmSetup">
		/** UsptStdrTaxitmSetupDao.selectList */
		select
			wct_taxitm_id
			, stdr_bsns_cd
			, wct_ioe_id
		from uspt_stdr_taxitm_setup
		where
			stdr_bsns_cd = #{stdrBsnsCd}
	</select>
</mapper>