<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="aicluster.pms.common.dao.UsptBsnsChklstDao">

	<insert id="insert">
		/** UsptBsnsChklstDao.insert */
		insert into uspt_bsns_chklst
								(
									chklst_id
									, bsns_cd
									, chklst_cn
									, creator_id
									, created_dt
									, updater_id
									, updated_dt
								)
						values	(
									#{chklstId}
									, #{bsnsCd}
									, #{chklstCn}
									, #{creatorId}
									, #{createdDt}
									, #{updaterId}
									, #{updatedDt}
								)
	</insert>

	<insert id="insertList">
		/** UsptBsnsChklstDao.insertList */
		insert into uspt_bsns_chklst
			(
				chklst_id
				, bsns_cd
				, chklst_cn
				, creator_id
				, created_dt
				, updater_id
				, updated_dt
			) values
		<foreach item="item" collection="list" separator=",">
			(
				#{item.chklstId}
				, #{item.bsnsCd}
				, #{item.chklstCn}
				, #{item.creatorId}
				, #{item.createdDt}
				, #{item.updaterId}
				, #{item.updatedDt}
			)
		</foreach>
	</insert>

	<update id="update">
		/** UsptBsnsChklstDao.update */
		update uspt_bsns_chklst
		set
			chklst_cn = #{chklstCn}
			, updater_id = #{updaterId}
			, updated_dt = #{updatedDt}
		where
			chklst_id = #{chklstId}
			and bsns_cd = #{bsnsCd}
	</update>

	<delete id="delete">
		/** UsptBsnsChklstDao.delete */
		delete from uspt_bsns_chklst
		where
			chklst_id = #{chklstId}
			and bsns_cd = #{bsnsCd}
	</delete>

	<select id="selectList" resultType="aicluster.pms.common.entity.UsptBsnsChklst">
		/** UsptBsnsChklstDao.selectList */
		select
			chklst_id
			, chklst_cn
		from uspt_bsns_chklst
		where
			bsns_cd = #{bsnsCd}
	</select>

</mapper>